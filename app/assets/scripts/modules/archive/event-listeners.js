class EventListeners {
    constructor() {

        /*
        $(window).resize(function() {
            $(window).trigger("window:resize")
        });
        */

    }
}
export default EventListeners;