<?php//suggesting this variables$format = 'm/d/Y';$tdate = (isset($specific_date) && $specific_date) ? $specific_date : null;  //"11/10/2016";$todayDate = new DateTime($tdate);//$todayDate = new DateTime();$todayDate_sql = Date_format($todayDate, $format);//print $todayDate_sql . "\n";$todayDate = strtotime($todayDate_sql);$todayDate = new DateTime($tdate);$todayDate->modify('-364 day');$oneYearAgo_sql = $todayDate->format($format) . "\n";$oneYearAgo = strtotime($oneYearAgo_sql);$todayDate = new DateTime($tdate);$todayDate->modify('-730 day');$twoYearsAgo_sql = $todayDate->format($format) . "\n";//print $twoYearsAgo_sql;$twoYearsAgo = strtotime($twoYearsAgo_sql);$todayDate = new DateTime($tdate);$todayDate->modify('-1825 day');$fiveYearsAgo_sql = $todayDate->format($format) . "\n";$fiveYearsAgo = strtotime($fiveYearsAgo_sql);//date to be compared with// you need this to compare the dates, such as (date1 <= date2)$date1 = strtotime($todayDate_sql);  // you need this to compare the dates, such as (date1 <= date2)$date2 = strtotime($twoYearsAgo_sql);