This is a README created to keep track of all the quirky items that are good to know about bookshelf.

1. The CFC's in the controller directory have instance's stored in the application by FW1. FW1 needs to be reloaded in order for changes to these files to take affect. This is done by using the URL argument ?reload=true.
2. ORM can be reloaded using the URL argument ?initdb